using UnityEngine;
using System.Collections;

public class WheelBehaviour : BasePlayer {

	// Use this for initialization
	void Start () {
        //Debug.Log("Wheel" + name + " id" + player);
	}
	
	// Update is called once per frame
	void Update () {
	
	}
}
